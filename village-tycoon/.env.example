# Cloudflare Tunnel token (get from Cloudflare Zero Trust → Tunnels → Create tunnel).
TUNNEL_TOKEN=

# Optional: override backend URL (default = Render-hosted casino backend).
# VILLAGER_API_BASE=https://kairo-bot-hc22.onrender.com
